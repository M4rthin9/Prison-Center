# Copy to docker/.env — used by watchtower to pull private Docker Hub repos.
# Token: Docker Hub → Account Settings → Personal access tokens (Read-only is enough).
DOCKERHUB_USERNAME=m4rthin9
DOCKERHUB_TOKEN=
